@model Item

@{ Layout = null; }

<div class="container">
    <h4>Add/Update Customer</h4>
    <hr/>
    <div class="row">
        <div class="col-md-4">
            <form asp-action="AddOrEdit" asp-controller="Items" method="post">
            @* <form asp-action="Edit" asp-controller="Items"> *@
                @if (ViewBag.Name != "Add")
                {
                    <div class="form-group">
                        <label asp-for="Name" class="control-label"></label>
                        <input asp-for="Name" class="form-control"/>
                    </div>
                    <div class="form-group">
                        <label asp-for="ParentId" class="control-label"></label>
                        <input asp-for="ParentId" class="form-control"/>
                    </div>
                    
                    <button type="submit"> Изменить </button>
                }

                @* <div class="form-group"> *@
                @*     @if (ViewBag.Name == "Add") *@
                @*     { *@
                @*         <input type="submit" value="Add" asp-action="Create" class="btn btn-success btn-sm"/> *@
                @*     } *@
                @*     else *@
                @*     { *@
                @*         <input type="submit" value="Update" asp-action="Update" class="btn btn-success btn-sm"/> *@
                @*         <input type="submit" value="Cancel" asp-action="Index" class="btn btn-defaul btn-sm"/> *@
                @*     } *@
                @* </div> *@
            </form>
        </div>
    </div>
</div>